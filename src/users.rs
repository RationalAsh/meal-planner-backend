/// Struct to represent user information.
pub struct User {
    /// Username of the user.
    pub uid: String,
    /// Email of the user.
    pub email: String,
    /// Password of the user.
    pub password: String,
}
